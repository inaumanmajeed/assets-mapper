// Main module exports for assets-mapper
const {
  generateAssetsMap,
  watchAssetsMap,
  cleanupAssetsMap,
} = require('./generator');

module.exports = {
  generateAssetsMap,
  watchAssetsMap,
  cleanupAssetsMap,
};
